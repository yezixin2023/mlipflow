interface:
  display_name: "LAMMPS molecular dynamics"
  short_description: "Prepare and supervise fingerprinted MLIP LAMMPS CPU/GPU jobs"
  default_prompt: "$lammps-md Prepare, submit, or verify one explicit-model LAMMPS NVT/NPT workflow with a reviewed CPU/GPU input bundle, site-owned ssh-slurm execution, portable model binding, and bounded completion checks."
