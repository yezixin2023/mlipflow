interface:
  display_name: "ASE molecular dynamics"
  short_description: "Plan, restart, and supervise explicit-model MLIP ASE NVT/NPT jobs on ssh-slurm"
  default_prompt: "$ase-md Plan, submit, restart, or verify one explicit-model ASE NVT Langevin or isotropic MTK NPT trajectory with approved checkpoint salvage, an explicit model path, fresh scheduler attempts, and bounded post-run checks."
