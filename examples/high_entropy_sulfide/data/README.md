# 数据边界

本目录故意不包含真实结构、DFT 标签、轨迹、模型权重或势文件。示例只 replay 已审阅的小型合成 CSV，因此不会暗示任何外部数据或模型具有再分发许可。

示例体系的 M 子晶格严格写作 `Mn/Fe/Ni/Cu/Zn`；除这五种 M 元素外，化学环境只包含 `Li/P/S`，不包含 Co。CSV 中的数值和结构 ID 都是接口测试用的虚构值。

把真实项目接入 MLIPFlow 时，请通过 artifact 引用记录数据位置、内容指纹、来源、许可证和访问限制；不要把大文件或专有文件复制进项目模板。
